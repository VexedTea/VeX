#ifndef __SOLVER_HPP__
#define __SOLVER_HPP__

namespace VeX{

    struct Verlet_Solver{
        sf::Vector2f gravity = {0.f, 1000.f};

        void update(float dt){

        }

        void applyGravity(){
            for 
        }
    };

} // namespace VeX

#endif // __SOLVER_HPP__
